variables:
  - myTestKey: |-
      myMultiline1
      myMultiline2
  - cascVersion: OC CasC Version V4
 # - environment: dev
  - stage: ${STAGE}
  - namespace: ${NAMESPACE}
  - controller-casc-branch: ${CONTROLLER_CASC_BRANCH}
